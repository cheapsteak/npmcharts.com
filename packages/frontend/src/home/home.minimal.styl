.minimal
  .header-controls-wrapper,
  .sample-matches,
  .sub-heading,
  & footer
    display: none
  .page-header
    position: absolute
    z-index: 100
    border-bottom: 0
    background: rgba(255, 255, 255, 0.7)
    &:before
      display: none;
  .heading
    margin-left: 20px
    margin-top: 10px
  .chart
    .legend
      top: 70px
      font-weight: bold
      z-index: 200