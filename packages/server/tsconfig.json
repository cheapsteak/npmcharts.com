{
    "extends": "../../tsconfig.json",
    "include": ["bin/**/*", "routes/**/*", "views/**/*", "../utils/**/*"],
    "exclude": ["node_modules", "**/*.spec.ts", "**/*.test.ts"],
    "compilerOptions": {
        "paths": {
            "utils": ["../utils"],
        }
    }
}
